import { MaterialFilterPipe } from './material-filter.pipe';

describe('MaterialFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MaterialFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
