import { SpellFilterPipe } from './spell-filter.pipe';

describe('SpellFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SpellFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
